# ADR-0011 action item 3 — v1.9 catchable-fraction owner review (affirmative split, reconciled — round 5)

Status: engineering classification only; not a rescoring, not a release gate; owner sign-off pending;
**round-5-reconciled candidate awaiting a fresh independent review round**

Primary author: Claude Sonnet AI (never human; authoring of this action item is reserved for Claude)
Owner signer (not co-author): Houman — see `owner_sign_off` in the JSON record, status `pending`
Machine-readable record: [`results/v1.9-catchable-fraction-owner-review.json`](../results/v1.9-catchable-fraction-owner-review.json)
Worktree base commit: `80ff9cf18933462c59e09b07ebf6b67a8dd8703c`
Candidates reviewed so far: round 1 `42304ec9d98862bafe18ffac52f28293a5f4430d`; round 2
`77e45e38daebfc57194a3472d51564c97ac25e99`; round 3 `c7d0986ab5e47960a4b22658aad47ce5f774ffff`;
round 4 `380b73f8290160fcc52f2e313c182da218e3d43f`; round 5 (exact)
`c30cbdd2e33c78cbff04e3509752cb0035f2c3ed` — ten isolated Codex AI passes across five rounds;
reconciled below

## Relationship to the immutable baseline

This note and its JSON record are a **new, separately authored document stacked on top of** the
immutable conservative baseline. They do **not** modify, reformat, or overwrite either:

- [`results/v1.9-catchable-fraction.json`](../results/v1.9-catchable-fraction.json) — raw byte
  SHA-256 `9e2b88ce295124b63ffe8333fffd1e248fb223b0a9059dc3fc5f8954483b797b`
- [`v1.9-catchable-fraction-2026-07-28.md`](v1.9-catchable-fraction-2026-07-28.md) — raw byte
  SHA-256 `36d429e57411397d51a7885dc06ad40aab142fadd4e743fae7ca858740370876`

Both files were re-hashed before this round's edits and confirmed unchanged; neither was edited.
The baseline's own **0/34 file-local** record is restated below as a **superseded floor**, not
rewritten in place.

## What this is

The baseline classified all 34 already-frozen, already-trusted `present` opportunities using
**only committed JSON metadata**. This owner-review document redoes the same 34-row
file-local/cross-module architecture question with **read-only source inspection** of the 8
implicated golden repositories (rounds 1–3), reconciled against four rounds of isolated Codex AI
review.

Round 4 opened **no golden repository and no new source file**, per instruction. It tested each
reviewer finding against content already recorded in this document's `rows[]` rationale text and
the frozen rule contract (`docs/packs/free-llm-v1-rule-contract.md`), and clarified a further
corollary of the full-chain standard adopted in round 3: a required element is not "resolved"
merely because a second file is named via a genuine import statement whose name or context
plausibly suggests it carries that element — the citation must be backed by content this document
actually read and quoted establishing the element, not by inference from an imported module's name
(e.g. `claim_extractor` sounding claim-extraction-related, or `ModelProber` sounding like it invokes
a model). A citation whose own recorded rationale explicitly admits uncertainty is, by that
admission, treated as unresolved rather than confident.

## Access discipline (verified, not merely asserted)

- Rounds 1–2 confirmed all 8 checkouts' `HEAD` against frozen commit SHAs and clean working trees
  before any file was read; 21 distinct evidence_scope anchor files were independently re-hashed and
  matched the frozen `whole_file_sha256`.
- Round 3 performed no new golden-repository reads. **Round 4 performed no new golden-repository
  reads and opened no new source file** — all findings were tested against this document's own
  already-recorded content and the frozen rule contract.
- No `git checkout`, `fetch`, `clone`, or network access was performed against any golden repository
  in any round. No golden repository's code was built, installed, or executed. No write, `touch`, or
  modification was attempted against any path under the golden helper directory.
- No `untouched-*` / `selection-untouched-*` file and no
  `calibration/llm/private/untouched-evidence.enc` was opened, listed, or referenced.
- No detector was run. No rule, manifest, label, hash, prior v1.9 result, or gate artifact was
  changed.

Full breakdown: `results/v1.9-catchable-fraction-owner-review.json` → `source_access_accounting`.

## Result (reconciled affirmative split — round 5)

**1/34 file-local; 7/34 confidently cross-module; 26/34 ambiguous/unresolved — split unchanged from
round 4; round 5 corrected accounting and rationale accuracy, not row classifications.**

This is still the fifth distinct split this document has carried: original candidate 0/34 local,
34/34 claimed confident; round 1 → 2/34 local, 25/34 confident, 7/34 ambiguous; round 2 → 1/34 local,
18/34 confident, 15/34 ambiguous; round 3 → 1/34 local, 17/34 confident, 16/34 ambiguous; round 4 →
1/34 local, 7/34 confident, 26/34 ambiguous (10 rows demoted). Round 5 reclassified no row.

**Round 5 reviews, against exact immutable candidate `c30cbdd2e33c78cbff04e3509752cb0035f2c3ed`:**

- **Pass A returned `PASS`** — confirmed mappings and the round-4 counts (1/7/26) as recorded.
- **Pass B returned `CHANGES_REQUIRED`**, with three findings, all independently re-tested against
  this document's own already-recorded content (no new source opened) and all three confirmed:
  1. `source_access_accounting.admissible_direct_import_files_supporting_confident_rows` had gone
     stale after round 4's demotions — it still credited `claim_extractor.py`, `lib/ai-pane-context.ts`,
     `models/model_loader.py`/`evaluation/benchmarks.py`, and `velune/models/probes.py` as
     "supporting confident rows" (all 5 files' only supported rows are now ambiguous) and stated
     count 7 when only 2 files (`runner.py`, `src/rag_engine.py`) actually still support a confident
     row. **Corrected**: those 5 files, plus `config.py` and `AgentRuntime.kt` (see below), moved
     into a new `admissible_citations_insufficient_for_confident_rows` bucket with a per-file note
     on the specific gap; the confident-support bucket recomputed to its accurate count of 2.
  2. The 4 confident Bryancruzcb `run.py` rows cited `config.py` alongside `runner.py`, but the
     recorded accounting never showed `config.py`'s own content being read or quoted — only
     `run.py`'s own lines referencing `config.DEFAULT_TARGET_PROVIDER`/`config.GEMINI_API_KEY`,
     which is the importing file's usage, not the imported file's content. This is the same
     import-name-only weakness round 4 rejected for other rows. **Resolved by the first of Pass B's
     three offered options**: `runner.py` alone — whose content this document already actually read
     in full in an earlier round (`build_summary` assembling both `target_label` via `target.py` and
     `judge_label` via `evaluators.py`) — already supplies the complete chain without `config.py`.
     `config.py` is dropped from all 4 rows' citations; classification is unchanged (still
     confident), since this is a citation narrowing, not a demotion.
  3. The `LLM-AGY-001` rationale for `3cf37c8f...`/`8fa3769...` incorrectly stated that the
     `AgentRuntime.kt` comment "establishes" the fourth required element (absence of a recognized
     gate). Re-reading the recorded text shows the comment states a Tier-B gate **is** handled (by
     native code) — it locates and discusses a gate-handling mechanism, which is a different fact
     from establishing the required *absence* of a recognized gate. **Corrected**: rationale now
     accurately describes the comment as locating, not establishing, the gate element (in fact, none
     of the four required `LLM-AGY-001` elements are established for these two rows — not even
     partially, on correct reading). Classification unchanged (already ambiguous since round 4).

**No Pass B finding was rejected.** As with round 4, Pass A's `PASS` is retained as an accurate
historical record of what that pass reported; it did not catch any of these three defects, which
Pass B found and this reconciling session independently confirmed by re-reading this document's own
already-recorded text. This is the second consecutive round with a `PASS`/`CHANGES_REQUIRED` split
between isolated passes reviewing the identical candidate.

Full round-5 findings, verification, and reconciliation method are in
`results/v1.9-catchable-fraction-owner-review.json` →
`review_and_reconciliation.rounds[4]`.

**Round 4 reviews, against exact immutable candidate `380b73f8290160fcc52f2e313c182da218e3d43f`:**

- **Pass A returned `PASS`** — confirmed all 34 id/rule/repository/commit/anchor/hash mappings, the
  round-3 split (1/17/16), full-chain treatment, hashes, scope-extension flags, and non-claim guards.
- **Pass B returned `CHANGES_REQUIRED`** — mechanical checks passed, but found the full-chain
  standard round 3 adopted (for the `001a4070...` fix) had not itself been applied consistently to
  10 rows still recorded confident: 2 `LLM-AGY-001` rows, 3 `LLM-DAT-001` rows, 2 `LLM-PRV-001` rows,
  and 3 `LLM-EVL-001` rows — each citing a second file by import statement and plausible name, but
  without the file's content ever having been read to confirm it actually establishes the required
  element. Pass B also flagged that this conflicts with this document's own companion Limitations
  statement that cited second components "do not themselves constitute a full trace" of that file's
  contents. Pass B's stated maximum under consistent treatment: 1 local / 7 confident / 26 ambiguous.

**Each of Pass B's 10 findings was independently re-tested** against this document's own
already-recorded rationale for that row and the applicable rule's required-evidence checklist — not
adopted on assertion, and not deferred to because Pass B's verdict outranked Pass A's. All 10 were
confirmed as genuine, pre-existing gaps:

- **2 `LLM-AGY-001` rows** (Shelly `_layout.tsx`, `3cf37c8f...` / `8fa3769...`): the rule requires
  four elements (tool definition/registration, an import-resolved side-effecting API, the model/agent
  exposure point, and absence of a recognized gate). The recorded rationale established only the
  fourth — via the anchor file's own comment naming `AgentRuntime.kt` — and even that file's content
  was never opened. The other three elements are unaddressed by any recorded text.
- **3 `LLM-DAT-001` rows** (Shelly `use-ai-pane-dispatch.ts`, `9cdd8cc0...` / `aae8e1b4...` /
  `b4e789e9...`): the data-path element is solidly established (`getTerminalSnapshotForSession` in
  `lib/ai-pane-context.ts`, a direct import), but the recognized-secret-source element rests on
  hedged language ("would actually originate or be filtered") rather than a confirmed source per the
  rule's strict definition (env/config/secret-manager keys or a classified literal credential —
  generic content does not qualify).
- **2 `LLM-PRV-001` rows** (`b202f9cf...` `graph.py`, `52a19bb1...` `models.py`): `claim_extractor.py`
  and `velune/models/probes.py` are genuine direct imports, but neither file's content was ever
  opened — the citations rest on the imported module's name and grouping. `52a19bb1...`'s own prior
  rationale had gone so far as to explicitly write "must be inspected to confirm the recognized local
  invocation," an admission round 3 carried forward without resolving.
- **3 `LLM-EVL-001` rows** (`app.py`, `970d551c...` / `02ccd751...` / `b6155037...`): case iteration
  and aggregate calculation are solidly confirmed — this document's earlier rounds actually read
  `evaluation/benchmarks.py`'s `run_benchmark` content. But the actual model-invocation binding was
  never confirmed: `app.py`'s own import block (already on record) directly imports
  `models.inference.ModelInference`, plausibly where the real invocation lives, but this document
  never cited it, opened `models/inference.py`, or confirmed the `app.py` call site binding the
  injected `generate_fn` callback to a real model call.

**No Pass B finding was rejected.** Contrast with the 7 rows that remain confident (4 Bryancruzcb
`run.py` rows, 3 `rag-qa` rows): their second-component evidence is content this document actually
read and quoted in full — `runner.py`'s `build_summary` assembling both `target_label` and
`judge_label`; `rag_engine.py`'s `_get_llm` returning a configurable `ChatGroq` model — a materially
different evidentiary weight than an import statement plus a plausible module name.

Full per-row rationale and both round-4 passes' complete findings are in
`results/v1.9-catchable-fraction-owner-review.json` → `rows[].affirmative_architecture_split` and
`review_and_reconciliation.rounds[3]`.

The 7 rows that remain confident, and why (as of round 5):

- **4 Bryancruzcb/aegis-eval-harness `run.py` rows** (`LLM-EVL-002`): `run.py` directly imports
  `runner.py`, whose actually-read content assembles both producer and judge identity before
  combining them. Round 5 dropped `config.py` from these rows' citation (see above) — `runner.py`
  alone already supplies the complete chain.
- **3 `rag-qa` rows** (`LLM-EVL-002`): the judge is defined locally in `evaluator.py` itself; the
  producer is resolved via `rag_engine.py`, a direct import whose relevant content was actually read.

## Scope extension (10 rows, pending owner acceptance)

Per action item 3, the explicit local-scope contracts (`LLM-PRV-001`, `LLM-EVL-001`, `LLM-EVL-002`)
cover 24 of the 34 rows. The remaining 10 rows apply the same file-local/cross-module question by
analogy. **Round 4 moves all 5 of the previously-confident scope-extension rows to ambiguous** (they
were exactly the Shelly `LLM-AGY-001`/`LLM-DAT-001` rows Pass B flagged). The breakdown is now:

- Confident (0): none.
- File-local (1): `sreyun`'s `e9feaddc...`.
- Ambiguous (9): all remaining scope-extension rows, including `memoturn`'s `976a81ef...`, `sreyun`'s
  `be731fdff...`, `Velune-CLI`'s `5e533efe...` and `ad6c075e...`, and now Shelly's `3cf37c8f...`,
  `8fa3769...`, `9cdd8cc0...`, `aae8e1b4...`, `b4e789e9...`.

**The extension of action-item-3 scope to these rules at all is not itself owner-approved regardless
of how any individual row resolved.** Each of the 10 rows carries `scope_extension_flag: true`, and
`owner_sign_off.scope_extension` in the JSON record lists all 10 row IDs and is `status: pending`.

## Owner sign-off

`owner_sign_off` in the JSON record names **Houman** as owner signer (not co-author — see
`primary_author`/`human_author`, which record this document as authored entirely by Claude Sonnet
AI, never human). `status`, `accepted_split`, `adjustments`, and `signed_at` are all `null`/`pending`
placeholders; no acceptance is asserted or implied by this document's publication. The 10-row scope
extension likewise awaits explicit owner acceptance before its derived-by-analogy classification can
be treated as an authoritative extension of action item 3.

## Independent Codex review and reconciliation (five rounds)

**Round 1** (`42304ec9d98862bafe18ffac52f28293a5f4430d`): two isolated Codex AI passes, both
`CHANGES REQUIRED`, reconciled to 2/34 local, 25/34 confident, 7/34 ambiguous.

**Round 2** (`77e45e38daebfc57194a3472d51564c97ac25e99`): two further isolated Codex AI passes, both
`CHANGES REQUIRED`, applying a strict per-row evidence boundary, reconciled to 1/34 local, 18/34
confident, 15/34 ambiguous.

**Round 3** (`c7d0986ab5e47960a4b22658aad47ce5f774ffff`): two further isolated Codex AI passes, both
`CHANGES REQUIRED`, adopting full-chain treatment, reconciled to 1/34 local, 17/34 confident, 16/34
ambiguous.

**Round 4** (exact immutable candidate `380b73f8290160fcc52f2e313c182da218e3d43f`): two further
isolated Codex AI passes — never human, neither seeing the other's conclusions from this round.
**Pass A returned `PASS`. Pass B returned `CHANGES_REQUIRED`**, finding that round 3's own full-chain
standard had not been applied to 10 rows. This reconciliation did not treat Pass A's `PASS` as
overriding or discounting Pass B's findings — each of Pass B's 10 findings was independently tested
against this document's own recorded evidence, all 10 were confirmed as genuine gaps, and none were
rejected. This is a real, recorded disagreement between two isolated reviewers of the identical
candidate: Pass A's `PASS` is retained as an accurate historical record of what that pass reported,
not evidence that round 3's output was actually correct — Pass B and this reconciling session's own
independent re-test both found real defects Pass A did not catch.

**Round 5** (exact immutable candidate `c30cbdd2e33c78cbff04e3509752cb0035f2c3ed`): two further
isolated Codex AI passes — never human, neither seeing the other's conclusions from this round.
**Pass A returned `PASS`. Pass B returned `CHANGES_REQUIRED`**, with three findings: stale
`source_access_accounting` still crediting round-4-demoted rows as confident-supporting; the 4
confident `run.py` rows citing `config.py` without its content ever having been read; and an
overclaim in the `LLM-AGY-001` rationale about what the `AgentRuntime.kt` comment establishes. All
three were independently re-tested against this document's own already-recorded evidence (no new
source opened, per instruction) and confirmed. No row was reclassified — `config.py` was dropped
from the 4 `run.py` rows' citation because `runner.py` alone, already read in full, was confirmed
sufficient on its own; the accounting bucket was recomputed; the AGY-001 rationale was corrected to
accurately describe what the cited comment does and does not establish. As with round 4, this is a
real `PASS`/`CHANGES_REQUIRED` split between isolated passes reviewing the identical candidate, and
Pass A's `PASS` did not catch what Pass B found.

Full per-round findings, verification methodology, and per-row decisions are in
`results/v1.9-catchable-fraction-owner-review.json` → `review_and_reconciliation.rounds[0]` through
`.rounds[4]`.

**This round-5 reconciliation is awaiting review at its eventual exact immutable commit.** No review
has yet run against the reconciled content represented by this artifact. `independent_review.status`
in the JSON record is `pending_rerun`, not `passed` or `approved` — a `PASS` from one isolated pass,
while the same round's other pass found and this session independently confirmed genuine defects, is
not treated as reviewer approval of this document. Status remains `pending_rerun` until two isolated
reviews run against that exact commit; no reviewer findings, verdicts, or pass results are invented
or forecast here for that round.

## Non-claim guards (restated)

- **Engineering classification only.** This document re-adjudicates no `ground_truth_label`,
  `evidence_scope`, or manifest hash. Every label from the frozen v1.9 golden artifacts is used
  exactly as frozen.
- **Not rescoring.** No detector rule, detector code, or existing v1.9 golden artifact is changed.
- **No performance claim.** The classification is an engineering-scoping inference from committed
  metadata plus read-only source inspection, not a recall, precision, or accuracy figure.
- **The immutable v1.9 NO-GO is intact.** `results/v1.9-golden-gate-no-go.json` remains the
  canonical, unmodified v1.9 release decision. This document is not bound into
  `release-thresholds.json`, `pre-result-commitment.json`, or any automatic-NO-GO evidence chain.
- **No detector run.** All classifications were produced by manual, read-only human-legible source
  inspection against the frozen rule contract (rounds 1–2), reconciled by document-internal
  cross-checking against already-recorded evidence (rounds 3–4), not by re-executing the free-llm-v1
  pack.
- **No untouched-cohort access or spend.** No `untouched-*`/`selection-untouched-*` artifact and no
  `calibration/llm/private/untouched-evidence.enc` was opened, listed, or referenced.
- **No golden relabeling.** No golden manifest, correction ledger, cohort file, or prior v1.9 result
  file was modified.
- **The baseline is unmodified.** `results/v1.9-catchable-fraction.json` and its review note remain
  byte-for-byte as committed; see the hashes above.

## Limitations

- Second-component citations identify the specific file/module the required evidence chain needs;
  they do not themselves constitute a full trace of that second file's contents in every case. Round
  4 sharpened this into an operative rule rather than only a caveat: a citation resting on an
  imported module's name/context, without its content having been read to confirm the required
  element, is not sufficient for a confident classification and is now recorded ambiguous.
  `source_access_accounting` in the JSON record distinguishes files whose content was actually read
  from files only confirmed to exist by name or import.
- This document does not attempt to resolve whether the original opportunity's `present` ground
  truth is correct — only where, architecturally, the decisive evidence for the applicable rule
  physically lives, and only using evidence admissible under the strict per-row boundary and
  full-chain treatment described above.
- The 10 rows outside the three explicit action-item-3 contracts remain pending explicit owner scope
  acceptance regardless of how confidently their architecture question was answered here — and, as of
  round 4, none of the 10 currently resolve to confident cross-module at all (9 ambiguous, 1
  file-local).
- 26 of 34 rows are recorded ambiguous. This is an honest reporting of evidence gaps — most because
  the strict boundary excludes the only second-component evidence found, because only part of a
  multi-element required-evidence chain could be admissibly resolved, or because a citation rested on
  an imported module's name rather than its actually-read content — not a claim that these rows are
  actually file-local.
- Five full rounds of Codex reconciliation, each independently re-verifying the prior round's own
  output, have each found real defects in what the previous round had asserted as reconciled and
  correct — including, in round 4, a defect (import-name-only citations) that had been present since
  round 1 for some rows, and in round 5, a document gone stale immediately after round 4's own edits
  (the accounting section) plus a citation weakness in the very rows round 4 left untouched
  (`config.py` on the `run.py` rows) and an overclaim in round 4's own corrective rationale
  (`AgentRuntime.kt`). Both rounds 4 and 5 were caught only by a reviewer whose own round's companion
  pass returned `PASS`. This is disclosed as a reason for reduced, not increased, confidence in the
  current split: a sixth review round could plausibly find further defects, including among the 7
  rows still recorded confident or within the accounting/methodology text itself.
