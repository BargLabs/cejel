# ADR-0011 action item 3 — v1.9 catchable-fraction owner review (authorized 24-row publication; unadmitted extension retained)

Status: engineering classification only; not a rescoring, not a release gate; **round 9 is an
adversarial source-correction pass on top of round 8's PASSED state; it changed row classifications
and evidence content; two fresh, independent, isolated Codex AI reviews (never human) each
independently returned PASS with no findings against the exact immutable candidate commit
`080cdc37a3bb3d6bf37ad8f7eafe51c7483297b5`, restoring `independent_review.status` to `passed`
against that exact commit; the 2026-08-08 owner decision authorizes publication of the explicit
24-row population only and records the 10-row extension as unadmitted**

Primary author: Claude Sonnet AI (never human; authoring of this action item is reserved for Claude)
Owner signer (not co-author): Houman — see `owner_sign_off` in the JSON record, authorized scope
is the explicit 24 rows only
Machine-readable record: [`results/v1.9-catchable-fraction-owner-review.json`](../results/v1.9-catchable-fraction-owner-review.json)
Worktree base commit: `80ff9cf18933462c59e09b07ebf6b67a8dd8703c`
Current branch/head at time of this round: `claude/v1-9-catchable-fraction-owner-review` at
`2e37888070f7445d4b9e10a463dba4501b41f41f`
Candidates reviewed and PASSED through round 8 (historical): round 1 `42304ec9d98862bafe18ffac52f28293a5f4430d`;
round 2 `77e45e38daebfc57194a3472d51564c97ac25e99`; round 3 `c7d0986ab5e47960a4b22658aad47ce5f774ffff`;
round 4 `380b73f8290160fcc52f2e313c182da218e3d43f`; round 5 `c30cbdd2e33c78cbff04e3509752cb0035f2c3ed`;
round 6 `8844074f41ed3d286e6c66b069905c7e3995afe2`; round 7 `f7ab6faf190a9328fe7d8ea19bcdaacf676931c6`;
round 8 (historical PASS) `5faaf04547e8ea1e7be96332d20ce0eb92467d21`.
**Round 9 (this round) is now PASSED at its own exact immutable commit
`080cdc37a3bb3d6bf37ad8f7eafe51c7483297b5`, after two fresh, independent, isolated Codex AI
reviews (never human) each independently returned PASS with no findings against that exact
commit — nine completed, independently-reviewed rounds in total, eighteen isolated Codex AI
passes across those nine rounds.**

## Relationship to the immutable baseline

This note and its JSON record are a **new, separately authored document stacked on top of** the
immutable conservative baseline. They do **not** modify, reformat, or overwrite either:

- [`results/v1.9-catchable-fraction.json`](../results/v1.9-catchable-fraction.json) — raw byte
  SHA-256 `2a55b69418a12507fe20c35aacbde7a78e9fe25b923a531d2fa74655b43ea3fb`
- [`v1.9-catchable-fraction-2026-07-28.md`](v1.9-catchable-fraction-2026-07-28.md) — raw byte
  SHA-256 `a04436198e9954cc388811f93db1058e19841f73304c3f96ee0378b15f89e660`

PR #45 changes only the baseline wording to identify its existing 0/34 result as a conservative
floor derived by resolving every ambiguous row to the cross-module fallback; it does not alter its
rows, classifications, manifests, ledgers, or other frozen evidence. This PR is stacked on that
wording update.

## What this is

The baseline classified all 34 already-frozen, already-trusted `present` opportunities using
**only committed JSON metadata**. This owner-review document redoes the same 34-row
file-local/cross-module architecture question with **read-only source inspection**.

**Chronology (corrected in round 9 — this had gone stale in prior wording):** read-only golden
source inspection occurred in **rounds 1–2** (the original authoring pass and its first
reconciliation) and again in **round 9** (this source-correction pass). Rounds 3 through 8 opened
**no new source** — they worked entirely from already-recorded evidence and the frozen rule
contract, reconciling **eight** isolated-Codex review rounds in total (not four, not three). Round
8 reached `independent_review.status = passed` after two isolated Codex passes independently
returned `PASS` with no findings. Round 9 is a **new** pass, triggered by four adversarial findings
reported against existing PR #47 (external review, distinct from the round-1..round-8 isolated-Codex
sequence), that opened permitted-but-previously-unread direct-import files for 5 explicit-contract
rows and moved them from ambiguous to confident cross-module. This changed row content after round
8's PASS; two fresh, independent, isolated Codex AI reviews then each independently returned PASS
with no findings against round 9's exact immutable candidate commit
`080cdc37a3bb3d6bf37ad8f7eafe51c7483297b5`, restoring `independent_review.status` to `passed`
against that exact commit — see "Independent Codex review" below.

## Access discipline (verified, not merely asserted)

- Rounds 1–2 confirmed all 8 checkouts' `HEAD` against frozen commit SHAs and clean working trees
  before any file was read; 21 distinct evidence_scope anchor files were independently re-hashed and
  matched the frozen `whole_file_sha256`.
- Rounds 3–8 opened no new golden-repository source — all findings were tested against this
  document's own already-recorded content and the frozen rule contract.
- **Round 9** used a separately recreated read-only checkout,
  `/private/tmp/cejel-v19-owner-review-golden-20260729-r9` (matrix:
  `/private/tmp/cejel-v19-owner-review-golden-20260729-r9-matrix.json`), restricted to the evidence
  file and files it directly imports, only for the rows named or discovered by the audit below. HEAD
  and working-tree cleanliness were verified for all 3 relevant repositories before any read; the
  r9 checkout's copies of the 3 relevant anchor files (`graph.py`, `models.py`, `app.py`) were
  independently re-hashed and confirmed byte-identical to the `whole_file_sha256` already on record
  from the original checkout, confirming this is the same frozen content, not a different snapshot.
  3 new files were read: `packages/nexagauge-graph/ng_graph/nodes/claim_extractor.py`
  (harnexa/nexa-gauge), `velune/models/probes.py` (Surya-Hariharan/Velune-CLI), and
  `models/inference.py` (ozturkoktay/insurance-llm-framework) — plus additional, already-permitted
  content of `app.py` itself (the `generate_output` call-site closure and `ModelInference`
  instantiation). Full record with raw byte SHA-256 of every new read:
  `results/v1.9-catchable-fraction-owner-review.json` → `golden_repository_verification.round_9_source_correction_checkout`.
- **Corrected provenance (internal audit):** for both the rounds 1–2 checkout and this round's `-r9`
  checkout, the repository-hosting **orchestrator** — external to this authoring session, using its
  own `checkout-frozen-cohort.mjs` tooling — recreated and hash/HEAD-verified the frozen golden
  checkout and matrix *before* this session read any source. **Within this authoring/source-reading
  session itself**, no `git checkout`, `fetch`, `clone`, build, install, execution, or modification
  was performed against any golden repository, in any round — only read-only `Read`/`grep`/`wc`/
  `shasum` inspection against the already-recreated checkout, plus this session's own independent
  re-verification of `HEAD` and file hashes against what the orchestrator provided.
- No `untouched-*` / `selection-untouched-*` file and no
  `calibration/llm/private/untouched-evidence.enc` was opened, listed, or referenced.
- No detector was run. No rule, manifest, label, hash, prior v1.9 result, or gate artifact was
  changed.

Full breakdown: `results/v1.9-catchable-fraction-owner-review.json` → `source_access_accounting`.

## Authorized publication (24 explicit-contract rows)

**Within the authorized 24-row v1.9 population: 0 confirmed file-local, 12 confirmed
cross-module, 12 unresolved. Possible file-local range 0–12 of 24. The committed metadata-only
result of 0/34 is a conservative floor produced by resolving every ambiguous row to cross-module,
not a measurement of the file-local fraction.**

This is the only figure authorized for publication. It carries its population, confirmed counts,
and unresolved count together. The 10-row extension is retained below as an unadmitted record; no
figure derived from it is part of this publication.

### What changed this round: 5 rows reclassified from ambiguous to confirmed cross-module

Per adversarial finding #1, the artifact had left at least five explicit-contract rows ambiguous
because permitted direct-import files — named by the anchor file's own imports — were never opened.
Each was inspected this round; all five resolved to confirmed, non-ambiguous cross-module (none
resolved file-local: in each case the confirmed content shows the required chain spans multiple
files, not that it collapses into the anchor file alone):

- **`llm-opp-b202f9cf...`** (`harnexa/nexa-gauge`, `graph.py:863`, `LLM-PRV-001`): `graph.py`'s own
  direct import `.nodes.claim_extractor` was opened. Its content confirms a genuine recognized local
  model invocation — `get_llm(...)` (claim_extractor.py:52) followed by `llm.invoke(...)` (line 66) —
  before the node's result is returned, satisfying the rule contract's local-helper allowance.
  `graph.py:863` (`node_report`) itself contains no invocation. **Reclassified: confident
  cross-module**, citing `claim_extractor.py`.
- **`llm-opp-52a19bb1...`** (`Surya-Hariharan/Velune-CLI`, `models.py:1025`, `LLM-PRV-001`):
  `models.py`'s own direct import `velune.models.probes.ModelProber` was opened (this row's own
  prior rationale had explicitly admitted it "must be inspected to confirm"). Its content confirms
  `ModelProber.run_all_probes()` builds `InferenceRequest` objects and calls
  `await self.provider.infer(req)` (probes.py:102/124/146) — a concrete, confirmed invocation
  pattern. **Reclassified: confident cross-module**, citing `velune/models/probes.py`.
- **`llm-opp-970d551c...`, `llm-opp-02ccd751...`, `llm-opp-b6155037...`** (`ozturkoktay/insurance-llm-framework`,
  `app.py:1090/1091/1225`, `LLM-EVL-001`): `app.py`'s own direct import `models.inference.ModelInference`
  was opened, confirming `ModelInference.generate()` calls `self.pipeline(prompt, **gen_kwargs)`
  (inference.py:249) — a genuine local invocation. Additional, already-permitted content of `app.py`
  itself was read to confirm the call-site binding: the `generate_fn` passed to `run_benchmark` is
  the locally-defined `generate_output` closure, which calls
  `st.session_state.inference_engine.generate(...)`, where `inference_engine = ModelInference(model,
  tokenizer, ...)` (app.py:332). Combined with `evaluation/benchmarks.py`'s already-confirmed case
  iteration/aggregation (read in an earlier round), the full chain — invocation, iteration,
  aggregation, emission — is now admissibly established entirely from actually-read content.
  **Reclassified: confident cross-module** (cross-module because case iteration/aggregation is
  confirmed to live in `evaluation/benchmarks.py`, a different file from `app.py`, not because the
  invocation is absent from `app.py`), citing `models/inference.py` + `evaluation/benchmarks.py`.

**Audit of all remaining ambiguous rows for the same specific gap:** every one of the (then-)26
ambiguous rows was checked against `source_access_accounting` for any *other* named-but-unread
direct-import file. No additional row qualified: `lib/ai-pane-context.ts`,
`apps/console/src/lib/api.ts`, `velune/cli/rendering/markdown.py`, and `packages/server/src/ingest.ts`
were all already actually read in prior rounds and found insufficient on their own confirmed content
(not merely unread); `AgentRuntime.kt` is not a direct import at all (evidence-file-comment based);
the 4 `nexa-gauge` `LLM-EVL-002` rows and `llm-opp-001a4070...` have **no** candidate direct-import
file named for their missing (producer-identity) element at all — there is nothing to open. The 5
rows above were the complete set meeting the exact criterion. **No row was tuned toward a
predetermined number** — each reclassification decision was made independently from that row's own
confirmed content, and the audit converged on exactly 5 rather than being fit to a target.

Full per-row rationale, the complete audit record, and this round's reconciliation method are in
`results/v1.9-catchable-fraction-owner-review.json` → `rows[].affirmative_architecture_split` and
`review_and_reconciliation.rounds[8]`.

### The 12 rows now confirmed cross-module, and why

- **4 Bryancruzcb/aegis-eval-harness `run.py` rows** (`LLM-EVL-002`): `run.py` directly imports
  `runner.py`, whose actually-read content assembles both producer and judge identity before
  combining them (`config.py` dropped as an unneeded citation in round 5).
- **3 `rag-qa` rows** (`LLM-EVL-002`): the judge is defined locally in `evaluator.py` itself; the
  producer is resolved via `rag_engine.py`, a direct import whose relevant content was actually read.
- **`llm-opp-b202f9cf...`** (`LLM-PRV-001`) and **`llm-opp-52a19bb1...`** (`LLM-PRV-001`): see above.
- **`llm-opp-970d551c...`, `llm-opp-02ccd751...`, `llm-opp-b6155037...`** (`LLM-EVL-001`): see above.

### Prior-round history (rounds 4–8), condensed

**Round 4** (`380b73f8290160fcc52f2e313c182da218e3d43f`): Pass A `PASS`, Pass B `CHANGES_REQUIRED` —
found the full-chain standard round 3 adopted had not been applied to 10 rows (the same rows this
round's finding overlaps with in part, though round 4 moved them to *ambiguous*, and round 9 has now
resolved 5 of them to confident via actual reads). **Round 5** (`c30cbdd2e33c78cbff04e3509752cb0035f2c3ed`):
Pass A `PASS`, Pass B `CHANGES_REQUIRED` — corrected stale accounting, dropped an unneeded `config.py`
citation, corrected an `AgentRuntime.kt` overclaim. **Round 6** (`8844074f41ed3d286e6c66b069905c7e3995afe2`):
both `CHANGES_REQUIRED` — corrected a bucket-taxonomy misclassification for 2 files. **Round 7**
(`f7ab6faf190a9328fe7d8ea19bcdaacf676931c6`): both `CHANGES_REQUIRED` — corrected the same taxonomy
gap for a third file (`ingest.ts`). **Round 8** (`5faaf04547e8ea1e7be96332d20ce0eb92467d21`): both
`PASS`, no findings — `independent_review.status` reached `passed` for round-8's then-current
content (1/7/26 split). See `review_and_reconciliation.rounds[3]` through `.rounds[7]` for full
per-round detail, preserved as accurate historical record and not rewritten by this round.

## Unadmitted record — 10-row scope extension (not published)

Per action item 3, the explicit local-scope contracts (`LLM-PRV-001`, `LLM-EVL-001`, `LLM-EVL-002`)
cover 24 rows. The remaining 10 rows apply the same file-local/cross-module question by analogy.
They are retained here because the adjudication is real work, but the scope was not authorized for
publication and the adjudication occurred after the fact. **None of the 5 rows reclassified this
round are scope-extension rows** — the unadmitted record remains:

- Confident (0): none.
- File-local (1): `sreyun`'s `e9feaddc...` — **this is the document's only confirmed file-local row,
  and it is here, in the unapproved scope extension, not among the 24 explicit-contract rows**
  (adversarial finding #3). 0 of 24 explicit-contract rows are confirmed file-local.
- Ambiguous (9): `memoturn`'s `976a81ef...`, `sreyun`'s `be731fdff...`, `Velune-CLI`'s `5e533efe...`
  and `ad6c075e...`, and Shelly's `3cf37c8f...`, `8fa3769...`, `9cdd8cc0...`, `aae8e1b4...`,
  `b4e789e9...`.

**The extension is not rejected as wrong; it is not admitted on evidence produced after the fact.**
Its sole confirmed file-local case is in this extension (`1 / 0 / 9`), not the authorized population
(`0 / 12 / 12`). Admitting the extension after learning it contains the only positive would be
post-hoc scope selection. Each row remains recorded with `scope_extension_flag: true`; neither the
extension nor any figure derived from it is published.

## Owner sign-off

`owner_sign_off` in the JSON record names **Houman** as owner signer (not co-author — see
`primary_author`/`human_author`, which record this document as authored entirely by Claude Sonnet
AI, never human). The 2026-08-08 decision accepts the explicit 24-row split for publication and
records the 10-row extension as unadmitted: it is retained, not deleted, but is not an authorized
population and cannot supply a published figure.

**`independent_review.status: passed` does not imply, substitute for, or modify either
`owner_sign_off.status` (pending Houman's acceptance of the affirmative split and any adjustments)
or `owner_sign_off.scope_extension` (which records the 10-row analogy extension as unadmitted).**
Independent review is an engineering verification process carried out entirely by isolated AI
reviewers (never human) against this document's mechanics and content; owner sign-off is Houman's
own, separate decision whether to accept the architectural classification, its method, its authorized
scope, and any adjustments required before it can be treated as authoritative for action item 3.
A `passed` independent review (round 8's historical state, and round 9's restored state) verifies
only that the document's evidence handling, rule application, and internal consistency met the
isolated-Codex verification standard at that round's exact immutable commit — it does not validate,
approve, or replace the owner's pending acceptance of either the split itself or the 10-row scope
extension.

## Independent Codex review and reconciliation (nine rounds completed and PASSED)

**Rounds 1–8** ran the isolated-Codex review sequence to completion, reaching `passed`:

- **Round 1** (`42304ec9d98862bafe18ffac52f28293a5f4430d`): both `CHANGES REQUIRED` → 2/34 local,
  25/34 confident, 7/34 ambiguous.
- **Round 2** (`77e45e38daebfc57194a3472d51564c97ac25e99`): both `CHANGES REQUIRED`, strict per-row
  evidence boundary adopted → 1/34 local, 18/34 confident, 15/34 ambiguous.
- **Round 3** (`c7d0986ab5e47960a4b22658aad47ce5f774ffff`): both `CHANGES REQUIRED`, full-chain
  treatment adopted → 1/34 local, 17/34 confident, 16/34 ambiguous.
- **Round 4** (`380b73f8290160fcc52f2e313c182da218e3d43f`): Pass A `PASS`, Pass B `CHANGES_REQUIRED`
  → 10 rows demoted to ambiguous → 1/34 local, 7/34 confident, 26/34 ambiguous.
- **Round 5** (`c30cbdd2e33c78cbff04e3509752cb0035f2c3ed`): Pass A `PASS`, Pass B `CHANGES_REQUIRED`
  → accounting/citation corrections, split unchanged.
- **Round 6** (`8844074f41ed3d286e6c66b069905c7e3995afe2`): both `CHANGES_REQUIRED` → taxonomy fix,
  split unchanged.
- **Round 7** (`f7ab6faf190a9328fe7d8ea19bcdaacf676931c6`): both `CHANGES_REQUIRED` → taxonomy fix
  (same defect class as round 6), split unchanged.
- **Round 8** (`5faaf04547e8ea1e7be96332d20ce0eb92467d21`): **both `PASS`, no findings** →
  `independent_review.status` reached `passed` for round-8's then-current 1/34/7/34/26/34 content.

Full per-round findings, verification methodology, and per-row decisions for rounds 1–8 are in
`results/v1.9-catchable-fraction-owner-review.json` → `review_and_reconciliation.rounds[0]` through
`.rounds[7]`, preserved as accurate historical record.

**Round 9 (this round) — reviewed and PASSED.**

Two fresh independent isolated Codex AI reviewers (never human) both returned PASS/no findings on
exact commit `080cdc37a3bb3d6bf37ad8f7eafe51c7483297b5`. They verified checkout provenance,
34 frozen mappings, the authorized explicit-contract subtotal (0/12/12 and 0–12/24), the retained
but unadmitted extension record (1/0/9), five corrected rows, the unresolved audit, chronology,
non-claim guards, JSON-Markdown consistency, and diff hygiene. `independent_review` restored to
`passed`; round-8 historical PASS covers old content only.

Reduced assurance remains due to unresolved mappings and old `976a81ef...` disagreement. The
authorized publication is limited to the 24 explicit-contract rows; the scope extension remains
unadmitted.

## Non-claim guards (restated)

- **Engineering classification only.** This document re-adjudicates no `ground_truth_label`,
  `evidence_scope`, or manifest hash. Every label from the frozen v1.9 golden artifacts is used
  exactly as frozen.
- **Not rescoring.** No detector rule, detector code, or existing v1.9 golden artifact is changed.
- **No performance claim.** The classification is an engineering-scoping inference from committed
  metadata plus read-only source inspection, not a recall, precision, or accuracy figure.
- **The immutable v1.9 NO-GO is intact.** `results/v1.9-golden-gate-no-go.json` remains the
  canonical, unmodified v1.9 release decision. This document is not bound into
  `release-thresholds.json`, `pre-result-commitment.json`, or any automatic-NO-GO evidence chain.
- **No detector run.** All classifications were produced by manual, read-only human-legible source
  inspection against the frozen rule contract (rounds 1–2, and round 9), reconciled by
  document-internal cross-checking against already-recorded evidence (rounds 3–8), not by
  re-executing the free-llm-v1 pack.
- **No untouched-cohort access or spend.** No `untouched-*`/`selection-untouched-*` artifact and no
  `calibration/llm/private/untouched-evidence.enc` was opened, listed, or referenced, in any round
  including round 9.
- **No golden relabeling.** No golden manifest, correction ledger, cohort file, or prior v1.9 result
  file was modified.
- **The baseline is unmodified.** `results/v1.9-catchable-fraction.json` and its review note remain
  byte-for-byte as committed; see the hashes above.

## Limitations

- Second-component citations identify the specific file/module the required evidence chain needs and
  must, as of round 4, be backed by content actually read (not merely an import statement plus a
  plausible module name) to support a confident classification. `source_access_accounting` in the
  JSON record distinguishes files whose content was actually read from files only confirmed to exist
  by name or import.
- This document does not attempt to resolve whether the original opportunity's `present` ground
  truth is correct — only where, architecturally, the decisive evidence for the applicable rule
  physically lives, and only using evidence admissible under the strict per-row boundary and
  full-chain treatment described above.
- The 10 scope-extension rows are retained as an unadmitted, post-hoc record regardless of how
  confidently their architecture question was answered here. Their numerator must not be blended
  into the authorized explicit-contract headline (0 confirmed file-local of 24).
- The authorized population has 12 unresolved rows. This is an honest reporting of evidence gaps — most because
  the strict boundary excludes the only second-component evidence found, because only part of a
  multi-element required-evidence chain could be admissibly resolved, or because the only candidate
  evidence has already been actually read and found insufficient (not merely unread) — not a claim
  that these rows are actually file-local. Its possible file-local range is 0–12 of 24.
- Nine full rounds of reconciliation (all nine now completed and independently reviewed), each
  independently re-verifying or re-examining the prior state, have repeatedly found real defects or
  genuine, permitted-but-undone evidentiary work: rounds 2 through 7 each found real, distinct defects
  in the prior round's own supposedly-reconciled output; round 8 found none; round 9 found that 5 rows
  the earlier rounds had conservatively left ambiguous could in fact be resolved using evidence the
  strict boundary had permitted all along, simply because it had not yet been opened, and two fresh
  independent reviews of round 9's reconciled state found no defects or evidence gaps. This is
  disclosed as a reason for continued caution, not increased confidence beyond what the confirmed
  counts and the possible range actually support: a future review could plausibly find
  further defects or further permitted-but-unopened evidence, including among the rows still recorded
  confident or ambiguous.
