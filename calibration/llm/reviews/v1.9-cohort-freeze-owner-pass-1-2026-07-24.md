# Free LLM v1.9 cohort freeze — owner pass 1

Reviewed at: `2026-07-24T22:56:00Z`
Reviewer identity: `codex-owner-v1.9-pass-1`
Review mode: first of two sequential AI passes requested by the solo owner

This is not human review. It reviewed only the selection and integrity artifacts; it did not
inspect repository source, labels, or detector results.

## Checks

- Validated the v1.9 cycle-reset ledger: 879 historical exclusions, the exact union of the v1.8
  reset (723), both v1.8 candidate cohorts (24 + 24), and the two real v1.8 selection proposals
  (`codex-v18-proposal-a-metadata-review`, 96 repositories; `v18-independent-metadata-review-b`,
  60 repositories) — verified byte-for-byte against their recorded hashes in
  `cohorts/selection-v1.8.json` before use.
- Confirmed the two v1.9 metadata proposals are disjoint from every identity folded into the v1.9
  historical exclusion ledger: no v1.9 selection proposal, or portion of it, was used to build the
  ledger that excludes candidates from selection. This is the specific failure mode that produced
  the v1.7 selection-boundary NO-GO and is not repeated here.
- Validated both fresh GitHub metadata-only proposals independently: no source access, cloning,
  labels, or detector-result access; required non-archived, non-fork metadata evidence is present;
  65 and 60 candidate repositories respectively, with zero internal overlap.
- Confirmed both selected cohorts contain 24 repositories with exact locked surface quotas,
  language minima, and Anthropic, OpenAI, and local/open-model coverage.
- Confirmed zero historical and cross-cohort overlap and that untouched binds the golden sibling
  candidate document and selection record.
- Confirmed the v1.9 discovery-anchor contract (corroborated candidate grouping, minimum three
  distinct query recipes) is locked before any source access, with
  `detector_results_seen_before_lock: false` and `source_accessed_before_lock: false`.
- Ran `validate-calibration.mjs`; it reported valid v1.9 selection, locked thresholds, and
  immutable freeze pending.

## Result

Approved for immutable metadata freeze only. Source checkout, opportunity review, detector
freeze, detector execution, experimental release, and public release remain unapproved.
