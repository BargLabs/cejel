# Free LLM v1.9 cohort freeze — owner pass 2

Reviewed at: `2026-07-24T22:56:30Z`
Reviewer identity: `codex-owner-v1.9-pass-2`
Review mode: second sequential AI pass requested by the solo owner

This is a separate defect-checking pass by the same Codex task, not independent or human review.
It remained limited to metadata and selection artifacts.

## Adversarial replay

- Replayed golden selection from the two private v1.9 metadata-only proposal files, the exact
  v1.9 reset ledger, policy ID, event identifier, timestamp, and selector source hash.
- Replayed untouched selection while binding the replayed golden candidate and selection record.
- Compared both replayed candidate documents and selection records byte-for-byte with the v1.9
  selected artifacts; both replays were exact.
- Independently recomputed the v1.9 historical exclusion union from `v1.8-cycle-reset.json`,
  `golden-candidates-v1.8.json`, `untouched-candidates-v1.8.json`, and the two real v1.8 selection
  proposal files, and confirmed the recorded 879-identity, sorted, deduplicated union hash matches.
- Confirmed the two v1.9 selection proposals do not appear anywhere in the v1.9 historical
  exclusion ledger's source bindings, so this reset does not exclude the same proposals it was
  meant to select from.
- Re-ran the selector and cycle-reset tests and `validate-calibration.mjs` successfully.
- Confirmed the release thresholds and detector rules were not changed by the v1.9 reset.

## Result

Approved for immutable metadata freeze only. This pass does not approve labels, a trusted golden
run, detector freeze, untouched execution, experimental release, or public release.
