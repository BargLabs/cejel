# v0.3.0 orphaned provenance record — 2026-08-07

**Status:** release-provenance event record; not part of the sealed v50 artifact closure.

The annotated `v0.3.0` tag points to
`aaa6771eb42acb79714a44a865517e2c8565f297`. It remains in place and was
never published to npm.

The failed npm publish from that tag created a detached build-provenance entry
at Rekor index `2365690569`. That entry truthfully binds the build performed by
the failed release run to the `v0.3.0` source tag and commit; it does not claim
that an npm package release exists. No `@cejel/cejel@0.3.0` package was
published.

`@cejel/cejel@0.3.1` is the first published Cejel release with build
provenance. Its SLSA provenance attestation names commit
`c48778d54444870a8185d09bdfe5b966ff309221`, ref `refs/tags/v0.3.1`, workflow
`.github/workflows/publish-npm.yml`, and Rekor index `2365972529`.

Historically, no earlier published Cejel version has a build-provenance
attestation. The npm `gitHead` field is absent for versions `0.1.5` through
`0.2.0`. Version `0.2.1` names its public patch commit, but that field is not
build provenance. Version `0.2.2` names
`e645faeb0722e47ee6eeaa16f6fe77790dc20387`, which is not an ancestor of the
public `origin/main` history. Those earlier artifact/source questions are
therefore not answered by a public build-provenance binding; they become
answerable from `0.3.1` onward.

The public `0.3.1` provenance can be retrieved and inspected with:

```bash
npm view @cejel/cejel@0.3.1 --json
curl -s "https://registry.npmjs.org/-/npm/v1/attestations/@cejel%2fcejel@0.3.1"
```
