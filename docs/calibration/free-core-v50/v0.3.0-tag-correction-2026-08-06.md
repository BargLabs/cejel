# v0.3.0 tag correction — 2026-08-06

**Status:** release-provenance event record; not part of the sealed v50 artifact closure.

The annotated `v0.3.0` tag was first created at commit
`de8412a511e6422f6e512e1ccc0d379864bb4cb9`, the merge of the coordinated
release-preparation change. A verify-only npm workflow dispatched from that tag
confirmed that `GITHUB_REF`, `GITHUB_SHA`, the tag commit, and checked-out HEAD
were the same commit. It then stopped before build or publication because the
new containment check could not resolve an older required commit in the
checkout's shallow history.

The failure did not establish that the tag lacked the required fixes: the tag
commit contains `e4283ba` (#96), `e50f531` (#98), and `f8f0b23` (#94). It
established only that the check could not evaluate those relationships with the
objects available in that checkout.

After the full-history checkout and distinct missing-object diagnostics merged
in #101 at `aaa6771eb42acb79714a44a865517e2c8565f297`, the unused remote
`v0.3.0` tag was deleted and recreated at that corrected commit. This was a
strict fast-forward: `aaa6771eb42acb79714a44a865517e2c8565f297` contains
`de8412a511e6422f6e512e1ccc0d379864bb4cb9`, so no release commit was
orphaned. The tag must contain the workflow it is dispatched from; otherwise a
later dispatch would run the earlier shallow-checkout workflow and fail in the
same way.

No npm package, OCI image, MCP Registry metadata, binary release artifact,
attestation, or provenance statement was published from either tag state. The
corrected tag still contains #96, #98, and #101. A new verify-only dispatch is
required before any publication and its identity values must be evaluated
independently of the earlier stopped run.
