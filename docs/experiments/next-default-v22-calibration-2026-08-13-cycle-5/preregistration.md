# Next calibrated default candidate v22 — public cycle-5 preregistration

Status: **preregistered before any cycle-5 candidate-universe metadata query, identity selection,
order freeze, source acquisition, scan, review, label, or estimate**

**CONSTRAINTS-VERSION: 2026-08-01.4**

## Claim boundary

This redacted document is the public counterpart to the fresh private cycle-5 authorization. It
does not amend or retry the four retired v22 waves, and it reveals no seed, candidate identity,
candidate metadata result, source, scan, review packet, label, or estimate.

`witan-rubric-v17-2026-07-24` remains the sole calibrated public default. Its published figures
remain v17-only. Prospective `witan-rubric-v22-prospective-2026-08-10` inherits none of them; only
a later terminal GO and separately dated promotion decision could license a bounded v22 statement.

## Immutable cross-repository binding

The private normative authorization merged in Alfred at
`d05df18fd3968b9ab5594a3997f74c577e574dd2` (PR #940). Its normative artifacts are:

| Private artifact | Git blob |
| --- | --- |
| `docs/calibration/next-default-v22-2026-08-13-cycle-5/preregistration.md` | `8f86fb86eb8ee0dba93478d346f5778d22b792d3` |
| `docs/calibration/next-default-v22-2026-08-13-cycle-5/selection-spec.json` | `65cb45b99bf4d8c15aa940aae463ff433c2dbd93` |

Any order-freeze harness must authenticate this Cejel merge/document blob and the Alfred
merge/two private blobs as cross-repository immutable content bindings. Git ancestry is asserted
only inside the repository containing each asserted commit.

## Frozen boundary and permitted stage

The private specification freezes Cejel commit `16bcf52abb6d56c4d386305eae6b984f7a10988e`, tree
`0c3599a236ad35acda48555e79b6f8d2a28dfabe`, and package `0.4.1` under the prospective v22 rubric
and `container-network-none-plus-node-runtime-deny-hook-v4` execution policy. Its explicitly
named v22 driver, freezer, and independent verifier are part of the private binding. The ordinary
public CLI remains v17 by default; this does not change it.

After both counterpart merges, one metadata-only order freeze may occur only after a separately
pushed harness independently authenticates all bindings, clean output absence, and its remote ref.
It may run only the committed GitHub metadata frame; it must not clone, read, scan, review, label,
or estimate any candidate source. The independent verifier must derive a receipt from a complete
artifact closure, while missing output or a binding failure produces a named independent attempt
record and nonzero failure. No retry is authorized.

This public authorization permits no source acquisition, measurement, calibration claim, default
promotion, or pooled v17/v22 figure.
