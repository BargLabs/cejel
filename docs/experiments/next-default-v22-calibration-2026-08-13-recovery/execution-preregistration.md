# Next calibrated default candidate v22 — public recovery execution preregistration

Status: **preregistered before any ordered candidate repository source is acquired, decoded,
scanned, reviewed, labelled, or estimated**

**CONSTRAINTS-VERSION: 2026-08-01.4**

## Claim boundary

This redacted record is the public counterpart to Alfred's new private execution-stage
authorization. It does not amend or retry the retired v22 instrument records; it exposes no
candidate identity, source, frozen revision, scan output, review packet, label, or estimate.

`witan-rubric-v17-2026-07-24` remains the sole calibrated public default. Its published figures
remain v17-only. The prospective candidate,
`witan-rubric-v22-prospective-2026-08-10`, inherits no precision, recall, false-positive-rate,
agreement, abstention, calibration, or promotion claim from this authorization. This stage is
only evidence collection for a later separately preregistered review and estimation stage.

## Immutable cross-repository binding

The private normative authorization merged in Alfred at
`ff4732969ad866d7cf27364535a342ae7b7ff343` (PR #925). Its two normative artifacts are:

| Private artifact | Git blob |
| --- | --- |
| `docs/calibration/next-default-v22-2026-08-13-recovery/execution-preregistration.md` | `2d065d2a6aeaaf6ce0bd370e219a9992621cbe62` |
| `docs/calibration/next-default-v22-2026-08-13-recovery/execution-spec.json` | `92cc22a485c8bd1d4a3f043ff9d80bcb6a65b0f9` |

The private execution specification in turn binds the completed metadata-only order closure at
Alfred merge `137899f7f5cd03fb5eb75ed209749ac28b0a473a`, its independently verified receipt, and
this repository's already merged redacted order record at
`1849d5b4ff3d6dff84f2478107a6003fc03a7810`, blob
`33673f6ecb8ae220b0c75a3544f6ffebf8d94c11`. Cross-repository binding is by immutable
commit-and-blob content; ancestry assertions apply only within each repository.

## Frozen candidate and permitted action

The candidate remains Cejel commit `52d174ecc9b89af9387d1586f8d87dda9151a31c`, tree
`348cd5f1fadfcd00b5e90e6dc7c1057ad1c6de55`, package `0.4.0`, with its committed explicit v22
launcher and v4 `container-network-none-plus-node-runtime-deny-hook-v4` policy. The normal public
CLI remains bound to v17; this is not a public CLI-default change.

After both counterpart documents have merged, one separately pushed harness may authenticate every
binding, build the exact candidate offline, and run two synthetic pre-source controls: the declared
no-egress probe under Docker `--network none`, and the v22 launcher plus independent artifact
verifier on a throwaway fixture. Only if both pass may it traverse the existing private 200-position
order once and acquire/scan each frozen revision. There are no replacements, reordering, extra
waves, or retries after traversal begins.

No repository program, hook, package script, import, test, build, notebook, or generated program
may run. Detector and source mounts are read-only; the scan output is the only writable mount.
Every acquisition and scan outcome is recorded in the private hash-closed execution record. A
binding, build, control, safety, scan, artifact, receipt, replacement, or retry failure is a
terminal instrument NO-GO and makes no statement about v22 quality.

This record authorizes no review, labels, adjudication, estimates, calibration result, published
figure, or default promotion.
