# Next calibrated default candidate v22 — public recovery preregistration

Status: **preregistered before any candidate-universe metadata query, identity selection, order
freeze, source acquisition, scan, review, label, or estimate**

**CONSTRAINTS-VERSION: 2026-08-01.4**

## Claim boundary

This redacted document is the public counterpart to a fresh private authorization after the v22
terminal instrument NO-GOs. It does not amend or retry those retired records, and it discloses no
seed, identity, candidate metadata result, source, scan, review packet, label, or estimate.

The sole calibrated public default remains `witan-rubric-v17-2026-07-24`. Its published figures
remain v17-only. Prospective `witan-rubric-v22-prospective-2026-08-10` inherits none of them;
only a later terminal GO and separately dated promotion decision could license a bounded v22
statement.

## Immutable cross-repository binding

The private normative authorization merged in Alfred at
`b6767db8d6634e67259495acbad39f0e2dbd6ac7` (PR #922). Its normative artifacts are:

| Private artifact | Git blob |
| --- | --- |
| `docs/calibration/next-default-v22-2026-08-13-recovery/preregistration.md` | `178b391884a14bc6d86919bfd3439582a5687cf3` |
| `docs/calibration/next-default-v22-2026-08-13-recovery/selection-spec.json` | `80036d149fedfa1ab469c97f0a9d06744bc978a0` |

Any order-freeze harness must authenticate this Cejel merge/document blob and the Alfred merge/two
private blobs as cross-repository immutable content bindings. Git ancestry is asserted only inside
the repository containing each asserted commit.

## Frozen boundary and permitted stage

The private specification freezes Cejel commit `52d174ecc9b89af9387d1586f8d87dda9151a31c`, tree
`348cd5f1fadfcd00b5e90e6dc7c1057ad1c6de55`, and package `0.4.0` under the prospective v22 rubric
and `container-network-none-plus-node-runtime-deny-hook-v4` execution policy. Its separately
synthetically exercised freezer wrapper and independent verifier are part of the private binding.

After both counterpart merges, one metadata-only order freeze may occur only after a separately
pushed harness independently authenticates all bindings, clean output absence, and its remote ref.
It may run only the committed GitHub metadata frame; it must not clone, read, scan, review, label,
or estimate any candidate source. The independent verifier must derive a receipt from a complete
artifact closure, while missing output or a binding failure produces a named independent attempt
record and nonzero failure. No retry is authorized.

This public authorization permits no source acquisition, measurement, calibration claim, default
promotion, or pooled v17/v22 figure.
