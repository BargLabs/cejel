# Next calibrated default candidate v22 — public cycle-7 preregistration

Status: **preregistered before any cycle-7 candidate-universe metadata query, identity selection,
order freeze, source acquisition, scan, review, label, estimate, gate evaluation, or promotion**

**CONSTRAINTS-VERSION: 2026-08-01.4**

## Claim boundary

This redacted document is the public counterpart to the fresh private cycle-7 authorization. It
does not amend or retry any retired v22 cycle, and it reveals no seed, candidate identity,
candidate metadata result, source, scan, review packet, label, estimate, or gate result.

`witan-rubric-v17-2026-07-24` remains the sole calibrated public default. Its published figures
remain v17-only. Prospective `witan-rubric-v22-prospective-2026-08-10` inherits none of them; only
completed blinded operator review, a later terminal GO, and a separately dated promotion decision
could license a bounded v22 statement. No v17 and v22 metrics may be pooled.

## Sole operator authority

Cycle 7 is authorized solely by the signed Alfred commit
`06e808fe5730d4986ce7bcfb2f8aeeef10b1dbac`, preserved by the authorization document merged at
`1e8e8bfad0a9bb9a4f20b4c306d5b2ea8c0c2b32` (PR #963). No authorization is inferred from a
prior preregistration, artifact, harness, terminal record, conversation, or agent action.

## Immutable cross-repository binding

The private normative preregistration merged in Alfred at
`91b42da5c2db04f9889342775a86612354d0f889` (PR #964). Its normative artifacts are:

| Private artifact | Git blob |
| --- | --- |
| `docs/calibration/next-default-v22-2026-08-14-cycle-7/preregistration.md` | `82d6d4a45ca25b650b6f2db42914ac5cbcc14eba` |
| `docs/calibration/next-default-v22-2026-08-14-cycle-7/selection-spec.json` | `21a2c3f83f21f1045490e84ab4d779b4239bef22` |
| `docs/calibration/next-default-v22-2026-08-14-cycle-7/control-anchors.json` | `13f7b5fa68ba2b621acb600f44ef76bd65e06a11` |

Any cycle-7 harness must authenticate this Cejel squash merge and document blob plus the Alfred
squash merge and all three private blobs as cross-repository immutable content bindings.
Source-branch commit identities have no authority. Git ancestry is asserted only inside the
repository containing each asserted commit.

## Frozen boundary and permitted stage

The private specification freezes Cejel commit
`41f784aa93038fb514ad6fdbab09a98e352c819c`, tree
`4324671bc0976d8d8f1565a5ea9d7d932a4e93b0`, and package `0.4.2` under benchmark
`cejel-next-default-v22-2026-08-14-cycle-7`, the prospective v22 rubric, and
`container-network-none-plus-node-runtime-deny-hook-v4` execution policy. That revision is
ancestry-proven to contain the explicit v22 driver, the no-egress timeout repair, and the repaired
metadata query and pre-boundary path-probe layer from Alfred #959. The named driver, freezer,
independent verifiers, frame, review/redaction protocol, estimators, and eighteen gates are private
content bindings. The ordinary public CLI remains v17 by default; this does not change it.

After both counterpart merges, the independently pushed harness must authenticate the frozen
boundary and both cross-repository bindings and apply the preregistered drift guard before any
one-shot. The private protocol permits one metadata-only order freeze and, only after its verified
receipt, one v4 no-egress execution ending at independently verified redacted review packets.

Both one-shots are operator-invoked from an external terminal in distinct, quiesced windows. No
agent may invoke either one-shot. A pre-boundary preparation stop spends neither one-shot.

This public counterpart reveals none of the private frame seeds or identities and licenses no
source disclosure, label, estimate, calibration figure, gate decision, terminal claim, default
promotion, or pooled v17/v22 figure. Blinded review and the packet-redaction audit remain operator
work after packet handoff.
