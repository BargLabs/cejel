# PARTIAL — DO NOT USE

This directory is a local execution-environment interruption, not a 200-repository run.

- Completed repositories: 27 of 200.
- Stop reason: the interactive session terminated the foreground scan process; background children
  are terminated by the same environment.
- Prohibited uses: packet generation, label partitioning, adjudication, metrics, or any claim about
  the 200-repository corpus.

A later workflow run must produce all 200 manifest positions and explicitly verify that count before
any downstream comparison uses its outputs.
