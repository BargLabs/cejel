# 0.4.9 release finish-line evidence

**CONSTRAINTS-VERSION: 2026-08-01.5**

This is a release-execution record, not an all-surfaces-current claim. The immutable release
commit is `cd75fc435d33bccc84293c8d1ce1a5a99685d6c0`. Never move `v0.4.9` or republish its npm
version. Operator-only steps follow `docs/release-process.md`.

## Expected values, before final readback

| Surface | Expected |
|---|---|
| npm | version and latest tag 0.4.9 |
| npm attestation | nonempty provenance naming the immutable release source |
| GitHub release | v0.4.9, draft=false, five binaries, five SBOMs, SHA256SUMS, signed provenance |
| git tag | v0.4.9 resolves to cd75fc435d33bccc84293c8d1ce1a5a99685d6c0 |
| OCI | 0.4.9 digest sha256:7b178ef67d3b5221739c6f3380d32d6db0098236831758a67d71dca80547925c; signed source is that release tag/commit |
| GitHub Action | immutable release and floating action@v1 execute 0.4.9 |
| Homebrew tap | all binary URLs name 0.4.9 and checksums match the release |
| MCP Registry | latest 0.4.9 and the same immutable OCI digest |
| cejel.dev homepage | current pinned invocations identify 0.4.9 |
| cejel.dev for-engineers | current marker 0.4.9; proof fields describe actual observed routes |
| changelog | newest entry 0.4.9; scoring and format changes distinguished |
| published-versions.json | OCI and MCP intended versions both 0.4.9 |
| leaderboard | a new published-package 0.4.9 run across all 24 unchanged corpus pins, three matching board surfaces, permanent withdrawal history intact |

An unreachable, empty or stale response fails its expectation. A historical board can pass the
currency verifier with a disclosed older scorer pin; that alone does not satisfy this card's
requirement to regenerate it on 0.4.9.

## Established release gap

On 2026-09-16, `https://registry.npmjs.org/@cejel%2fcejel/0.4.9` returned version 0.4.9,
with neither `gitHead` nor `dist.attestations`. Its attestation endpoint returned HTTP 404.
The published CLI itself executed as 0.4.9 and emitted the new Scope statement. Runtime
behaviour does not supply the missing source attestation. Do not claim all thirteen checks
are green. Carry npm provenance restoration in the next release; do not replace 0.4.9.

## Distribution jobs already executed

Binary run [35129237150](https://github.com/BargLabs/cejel/actions/runs/35129237150)
executed all five native build/verification jobs and `Guard 6 and attach to draft release`
successfully. Twelve draft assets were downloaded independently with `gh release download`.
`shasum -a 256 -c SHA256SUMS` passed for all five binaries. Each binary also passed
`gh attestation verify` against the attached bundle, restricted to `BargLabs/cejel`,
`refs/tags/v0.4.9` and source digest `cd75fc435d33bccc84293c8d1ce1a5a99685d6c0`.
The downloaded Darwin arm64 binary executed `--version` as 0.4.9. This verifies the draft
artifacts; public-URL checks and published-platform consumer checks still follow publication.
The draft is ready for the operator's `--draft=false` command below. Do not rebuild it.

Follow-up: the operator published the release at `2026-09-16T17:46:42Z`; independent
readback reports `draft=false` and twelve assets. The site's exact public download/checksum/run
recipe now passes on Darwin arm64. Published-native verification run
[35130282414](https://github.com/BargLabs/cejel/actions/runs/35130282414) executed Windows,
Linux aarch64 and npm against the same fixture: all three report digests are
`e051ff83100531fa8099b95a6deb17c81ae769d29a9c238dfe480dfee1bf072a`.
MCP-client run [35130285162](https://github.com/BargLabs/cejel/actions/runs/35130285162)
completed its diagnostic jobs, but its green status is not an all-routes-success claim:
OpenClaw add/probe passed on Node 24; add failed on Node 22.23.2; Smithery discovery worked
but anonymous add stopped at login. Preserve these route-specific limitations on the site.
Homebrew update is prepared in [tap PR #23](https://github.com/BargLabs/homebrew-tap/pull/23).

Run [35125939843](https://github.com/BargLabs/cejel/actions/runs/35125939843):
`preflight`, `publish-oci`, `resolve-mcp-oci-digest`, `publish-mcp-registry` and
`verify-mcp-oci-release-chain` executed successfully. `confirm-verify-only` and
`verify-mcp-registry-republish-path` were skipped. This run did publish OCI and MCP.
Do not repeat the non-reuse OCI publish against the already-published tag.

## Changelog correction

The release-tree rubric record already distinguishes four rows with metric changes from all
24 byte-different reports. The CLI changelog retained the pre-fingerprint count of 20
byte-identical reports. Correct the current documentation without changing the tagged tree.
React's observability count 68 → 108 and Alfred's 64 → 73 do not change either score.
The new report field and format 1.2 are format changes, not 24 scoring changes.

## Operator sequence

1. Read release state immediately before execution. While it still has zero assets:

   ```sh
   gh release edit v0.4.9 --repo BargLabs/cejel --draft
   gh workflow run release-binaries.yml --repo BargLabs/cejel --ref v0.4.9 -f release_tag=v0.4.9 -f attach_to_release=true
   ```

   Wait for all five native builds, set verification, and attachment to succeed. Verify assets
   and signed source identity before executing the separate final command:

   ```sh
   gh release edit v0.4.9 --repo BargLabs/cejel --draft=false
   gh workflow run verify-published-windows-binary.yml --repo BargLabs/cejel --ref v0.4.9 -f release_tag=v0.4.9
   gh workflow run verify-mcp-distribution-routes.yml --repo BargLabs/cejel --ref v0.4.9
   ```

2. Complete the five Action-major-tag checks in the release process, in order. The composite
   action has no diff between v0.4.8 and v0.4.9. OCI provenance independently names the new
   immutable tag/commit. The no-v1-binding audit and consumer execution remain required;
   neither a version declaration nor moving a ref substitutes for them.

3. Update the tap from verified published binary checksums, run its consumers, then finish
   the site's proof fields from those actual run results. No old proof run may be relabelled.

4. After the site PR is ready, dispatch its consumer workflow on that PR branch, read actual
   package and Action versions, and record the resulting run before merging. Deployment is
   operator-executed. Verify production content after deployment.

5. Dispatch the independent thirteen-surface verifier:

   ```sh
   gh workflow run verify-release-currency.yml --repo BargLabs/cejel -f version=0.4.9
   ```

   Record every result, including the npm provenance failure; do not weaken the verifier.

## Readback ledger — 2026-09-16

Expected values are the table above. These readings precede operator-only completion.

| Surface | Command / source read | Value read |
|---|---|---|
| npm | `curl -fsS https://registry.npmjs.org/@cejel%2fcejel/0.4.9`; published `npx --yes @cejel/cejel@0.4.9 --version` from neutral cwd | 0.4.9; no gitHead or dist.attestations |
| npm attestation | `curl` attestation endpoint named above, HTTP status captured | 404; FAIL |
| GitHub release | `gh release view v0.4.9 --repo BargLabs/cejel --json isDraft,assets` | draft=false, 0 assets; FAIL |
| git tag | `gh api repos/BargLabs/cejel/git/ref/tags/v0.4.9`, then resolve annotated tag; independent currency run 35126256923 | cd75fc435d33bccc84293c8d1ce1a5a99685d6c0 |
| OCI | `gh api repos/BargLabs/cejel/attestations/sha256:7b178ef67d3b5221739c6f3380d32d6db0098236831758a67d71dca80547925c`; decode public DSSE payload | resolved dependency refs/tags/v0.4.9 and the expected commit |
| GitHub Action | `gh api repos/BargLabs/cejel/git/ref/tags/v1`; `git diff v0.4.8 v0.4.9 -- action/action.yml` | v1 still 76063924da7a7f6ceabad526e0076d7d1769d188; composite diff empty; FAIL |
| Homebrew | `gh api 'repos/BargLabs/homebrew-tap/contents/Formula/cejel.rb?ref=main'`; decode content | all formula URLs still 0.4.8; FAIL |
| MCP Registry | `curl -fsS 'https://registry.modelcontextprotocol.io/v0.1/servers/io.github.BargLabs%2Fcejel/versions/latest'` | 0.4.9, expected OCI digest |
| homepage | `curl -fsS https://cejel.dev/` | pinned invocations 0.4.8; FAIL |
| for-engineers | `curl -fsS https://cejel.dev/for-engineers/`; independent currency run 35126256923 | current marker 0.4.8; FAIL |
| changelog | `curl -fsS https://cejel.dev/changelog/` | newest named release 0.4.8; FAIL |
| published-versions.json | `pnpm run validate:distribution`; independent currency run 35126256923 | intended OCI/MCP 0.4.9; metadata agreement passes, not proof of publication |
| leaderboard | `node scripts/verify-board-surfaces.mjs` | live three surfaces agree on 0.4.5, historical pin disclosed, zero structural violations; still awaiting 0.4.9 board deployment |

The full independent thirteen-surface run is
[35126256923](https://github.com/BargLabs/cejel/actions/runs/35126256923), read with
`gh run view 35126256923 --repo BargLabs/cejel --log-failed`. It reports six passing and
seven failing surfaces. The green leaderboard check validates its disclosed historical pin,
not the completion of the requested new corpus run.

## Board preparation and validation

The new board executed the published `@cejel/cejel@0.4.9` CLI on all 24 unchanged corpus
revisions, from neutral invocation directories. All 24 completed; all 24 retain the behaviour
fingerprint. Headline scores match the recorded release-candidate comparison. React reads 108
and Alfred 73 on `observability_depth`; A3 remains 2.3 and 3.6 respectively.

The mandatory public staging backstop scanned 100 files against 8,467 private-path candidates
and found zero hits. The staged three board formats pass `verify-board-surfaces.mjs`, including
the permanent withdrawal-history digest. The site separately renders its public certificates;
its presentation now includes tree scope, producer version and the recorded behaviour fingerprint.
No private raw reports or evidence locations enter this repository.

Cejel validation: build; 87 test files / 1,352 tests; explicit offline guard 41 tests;
distribution metadata validation. All passed. No non-test `src/witan/` source, calibration
record, B10 pin, immutable release tag or held PR #307 was changed.

## Action binding audit and fresh consumers

The public GitHub release inventory contains no release named `v1`. Read back 39 historical
release-bundle/npm-attestation endpoints: 29 answered and none of their decoded provenance
payloads bound `refs/tags/v1`. Ten npm endpoints returned 404 (0.4.9, 0.2.2, 0.2.0,
0.1.10, 0.1.9, 0.1.8, 0.1.7, 0.1.6, 0.1.4, 0.1.1). These are missing records, not affirmative
proofs of absence from every transparency service. The release process's no-prior-binding
confirmation remains an operator prerequisite; no tag was moved during this work.

Once the release checks and that confirmation pass, the exact operator ref update is:

```sh
gh api --method PATCH repos/BargLabs/cejel/git/refs/tags/v1 \
  -f sha=cd75fc435d33bccc84293c8d1ce1a5a99685d6c0 -F force=true
gh workflow run verify-cejel-consumer-routes.yml --repo BargLabs/cejel-site \
  --ref codex/cejel-049-finish-line
```

Read `action@v1`'s produced attestation runtime, not just its ref. If it does not execute
0.4.9, restore the observed prior v1 target `76063924da7a7f6ceabad526e0076d7d1769d188`
and investigate, as the release process requires.

The site PR automatically ran consumer verification as
[35128087532](https://github.com/BargLabs/cejel-site/actions/runs/35128087532): currency and
package clients passed. Pinned/latest/unpinned npx, pinned pnpm and Bun executed 0.4.9.
Unpinned pnpm executed 0.4.8 under the allowed release-age policy. Immutable and main Action
routes reported 0.4.9. The v1 route built 0.4.8 and failed its runtime assertion. The site
workflow now prints actual runtime before testing equality to preserve mismatches as evidence.

The proposed 0.4.9 download recipe stopped on HTTP 404 from the real release asset URL.
No checksum or execution success is claimed. Its `set -eu` correction also passed a negative
check: a wrong checksum stopped execution. Re-run against the real assets after publication.
