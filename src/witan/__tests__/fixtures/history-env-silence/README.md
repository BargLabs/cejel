# Synthetic non-template environment-file fixture

This repository is fully synthetic. The characterization tests create tracked,
non-template `.env*` files with non-placeholder content and assert that A2 emits the
expected path-only information finding in both the current tree and reachable history.
