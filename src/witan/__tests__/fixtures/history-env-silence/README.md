# Synthetic non-template environment-file fixture

This repository is fully synthetic. It reproduces a tracked non-template environment file
whose placeholder-only content currently suppresses the A2 path-only finding.
